{
    "title": "Canada Dentist Directory Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of search URLs to scrape.",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://www.yellowpages.ca/search/si/1/dentist/Toronto+ON" }
            ]
        },
        "maxLeads": {
            "title": "Maximum Leads",
            "type": "integer",
            "description": "Maximum number of Canadian dental leads to extract.",
            "default": 100,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration",
            "type": "object",
            "description": "Proxy settings. Apify Residential Proxy (Canada targeted) is required.",
            "prefill": { "useApifyProxy": true },
            "editor": "proxy"
        }
    },
    "required": ["startUrls"]
}
